<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/verdeEscuro</item>
        <item name="colorPrimaryDark">@color/verdeClaro</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!--
        <item name="backgroundColor">@color/azulEscuro</item>
        <item name="hintTextColor">@color/design_default_color_background</item>
        <item name="android:textColorLink">@color/design_default_color_background</item>
        <item name="android:textColorHighlight">@color/design_default_color_background</item>
        <item name="android:textColorHint">@color/design_default_color_background</item>
        <item name="android:color">@color/design_default_color_background</item>
        <item name="android:text">@color/design_default_color_background</item>
        -->

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
